let doTimes(times: Int, action: () -> Unit) {
    for (let i in 1..10) {
        action();
    }
}

let bruh = doTimes(3,
    () -> { print("hello world"); }
);

let l = () -> 5;

let main(args: String[]): Unit {
    print(l);
}